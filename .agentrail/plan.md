Goal: give hlasm a real diagnostic surface and a source-level way to break long-branch ranges, so RPG-II (and future consumers of hlasm-generated assembly) get useful errors from hlasm itself and can work around as24 'branch target too far' at the source instead of hand-written trampolines. Stay within the project's assembly-oriented ethos: no typing, no runtime model shift, compact output by default, every new behavior opt-in via SET-style switches. Prior structured-lowering branch-range hardening (commit b2526bb) already covers hlasm-generated branches; this saga targets user-written branches and the missing diagnostic surface. Keep ./build.sh test green and track every .agentrail/ change in git like source code.