Introduce the first real diagnostic surface for hlasm. Add an input source-location rail (file/buffer id + 1-based line number) that the main read path updates as it advances, and a new opt-in 'SET HLDIAG,1' switch. Add a central warn routine (e.g. _hldiag_warn) that, when HLDIAG is enabled, emits '; !! hlasm: <msg> at <file>:<line>' lines into the generated stream; when disabled, is a cheap no-op. Do not add any real callers yet beyond a single smoke usage that fires on a known-safe condition so the emission path can be proved with a demo. Document the switch next to HLANN / HLIST / HLXREF in README. Keep the full build.sh test suite green.