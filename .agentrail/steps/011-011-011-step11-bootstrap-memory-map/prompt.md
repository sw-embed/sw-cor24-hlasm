Define and implement the first explicit bootstrap-oriented memory map for hlasm. Reserve low SRAM for preloaded ASCII source/include buffers, reserve a middle-SRAM arena for assembler-owned runtime data such as source descriptors, symbols, and macro storage, and document or implement the preferred stack split with a 3K EBR stack plus SRAM fallback if needed. Keep the work target-native, compatible with cor24-run --load-binary and --patch, and add a concrete demo or regression proof for the chosen layout.