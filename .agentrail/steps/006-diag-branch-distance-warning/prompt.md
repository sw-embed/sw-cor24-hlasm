When HLDIAG is on, measure emitted-code distance to each local branch target and emit a warning for short-form bra/brt/brf/jal whose target distance exceeds a configurable threshold ('SET HLDIAG_BRANCH_WARN,<bytes>', default 0 = off). Goal: pinpoint exactly the call sites that should switch to the step-5 pseudos before as24 rejects them. A second pass is acceptable if it keeps the implementation simple. Add a demo that places a branch target just past the threshold and captures the warning.