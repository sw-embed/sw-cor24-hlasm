Define the first self-hosting milestone for sw-cor24-hlasm. Identify the minimum HLASM subset needed for hlasm to assemble a reduced assembler source in itself, document the blockers in current hlasm.s, and implement the next highest-leverage feature or source refactor that moves the codebase toward that self-hosting subset. Prefer structured IF lowering if it directly removes a blocker; otherwise create a bootstrap-oriented source split (for example hlasm0/hlasm1 layering) and add a concrete demo or proof point.