Extend the existing xref/symbol-table infrastructure (HLXREF) with an opt-in 'unresolved reference' pass that, when HLDIAG is on, emits a warning for each symbol referenced but never defined in the unit being assembled. Reuse the xref-collection rails from the HLXREF saga; do not duplicate the pass. Add a demo that references an undefined label and verifies the expected HLDIAG warning. No change when HLDIAG is off.