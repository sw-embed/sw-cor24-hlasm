Wire the pass-through / unknown-mnemonic path into HLDIAG (from the diag-baseline step). When hlasm encounters a token at mnemonic position that it neither recognizes as a keyword/directive/known mnemonic nor matches against a defined macro, emit an HLDIAG warning with the source location and the offending token, then continue to pass-through as today. Add a demo that exercises a deliberately-misspelled mnemonic under HLDIAG and captures the warning in a golden file. Do not change emission when HLDIAG is off.