Build the first named-include layer on top of the new INCBUF return stack. Define a tiny low-SRAM include-name table that maps short ASCII include names to preloaded source slots, add an INCLUDE name directive that resolves through that table without breaking existing SRCBUF/INCBUF numeric behavior, preserve the current config block and runtime arena map, and add a demo/regression proof that a patched main source performs a named include and then resumes from the caller source after include EOF.