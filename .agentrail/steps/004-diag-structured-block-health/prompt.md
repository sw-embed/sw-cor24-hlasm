When HLDIAG is on, surface structural warnings from the structured-control-flow lowering: unclosed IF/DO/SELECT at EOF, ELSEIF/ELSE outside IF, ENDDO/ENDIF/ENDSELECT without a matching opener, and (gated by 'SET HLDIAG_BODY_WARN,<n>', default 0 = off) a warning when any structured body exceeds <n> source lines. These are hints that downstream as24 may see a 'branch target too far'. Add demos for each warning class. No behavior change when the switches are off.