Make the primary source descriptor fully config-driven for bootstrap use. Extend the existing source config block so stage0 can override the main source buffer base and length, not just append extra buffers, while preserving the current defaults when no override is present. Keep the runtime arena map from Step 11 intact, stay compatible with cor24-run --load-binary and --patch, and add a demo/regression proof that assembles from a non-default main source window.