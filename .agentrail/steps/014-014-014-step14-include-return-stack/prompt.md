Build the first true include-return mechanism on top of SRCBUF. Add a small source-stack so an explicit include-style directive can push the current source position, switch to a target preloaded source buffer, and then return to the caller source when the included buffer reaches EOF. Preserve the current config format, runtime arena map, and directive-driven switching behavior, stay target-native with cor24-run --load-binary/--patch, and add a demo/regression proof that a patched main source includes a second buffer and then resumes emitting lines from the original main source afterward.