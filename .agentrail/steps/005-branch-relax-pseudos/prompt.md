Add source-level long-branch pseudo-ops that the user can invoke at specific far-call sites: 'HLJ <label>' (long-form unconditional jump), 'HLCALL <label>' (long-form call-and-link), and 'HLB<cond> <label>' for at least the common conditions used by structured lowering (HLBT, HLBF, plus equality/zero variants). Each expands to a trampoline sequence guaranteed to reach any 24-bit target (la rN,<label> + jmp rN or equivalent), using a caller-neutral scratch register consistent with the existing calling convention. Short bra/brt/brf/jal are left untouched -- this is explicit opt-in per call site. Add demos proving each pseudo reaches a target placed beyond the short-form range. Update README.