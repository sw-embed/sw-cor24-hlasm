Implement multi-buffer source input for sw-cor24-hlasm using the cor24-run model already used in sibling repos. Extend the source-descriptor logic so stage0 can read from more than one ASCII buffer in memory, with a small descriptor table or stack that can switch between the main source and additional loaded buffers. Add a concrete demo/proof point showing a main source buffer and at least one secondary buffer (include-ready, even if full INCLUDE syntax is deferred), and keep UART as the emitted plain-assembly output.