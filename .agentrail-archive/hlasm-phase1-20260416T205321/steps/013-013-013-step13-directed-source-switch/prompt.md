Build the first directive-driven source switch on top of the config-driven buffer table. Add a minimal HLASM directive that can advance to a specific preloaded extra source buffer by slot or simple name, so a bootstrap source can explicitly request the next include-style buffer instead of relying only on sequential EOF handoff. Preserve the Step 12 config format and runtime arena map, stay target-native with cor24-run --load-binary/--patch, and add a demo/regression proof that a main source at a patched address explicitly pulls in a second source buffer.