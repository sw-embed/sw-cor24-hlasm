Build an include-ready source switcher on top of the new multi-buffer input path. Replace the fixed second-buffer handoff with a small descriptor stack or table walker that can activate a named or indexed secondary source buffer without changing the UART output model. Add a concrete demo that models INCLUDE-like behavior using multiple preloaded ASCII buffers, and keep the path compatible with future self-hosting use of source and include files loaded via repeated cor24-run --load-binary flags.