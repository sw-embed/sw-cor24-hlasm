Add an opt-in HLXREF report for hlasm-generated output. Keep it text-only and grep-friendly: emit a compact end-of-run cross-reference section for copied members, macro definitions, macro expansions, and assembly-time symbols (SET/EQU) when SET HLXREF,1 is active. Keep normal output compact by default, add one focused demo/regression, update docs, and keep ./build.sh test green with tracked reg-rs artifacts current.