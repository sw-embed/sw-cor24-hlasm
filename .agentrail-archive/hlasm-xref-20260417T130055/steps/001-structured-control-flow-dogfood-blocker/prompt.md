Fix the structured control-flow blocker exposed by sw-cor24-rpg-ii dogfooding. Reproduce the failing generated output when structured IF/ELSEIF is used in parser descriptor-storage code, fix undefined _hlif_*_end labels and any remaining short-branch range failures in the structured lowering path, add a focused regression/demo that stresses the affected lowering shape, keep ./build.sh test green, and document any downstream repo changes needed without editing sibling repos.